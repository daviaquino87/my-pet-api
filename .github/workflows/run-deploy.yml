name: Deploy app
on:
  push:
    branches:
      - realese/homolog

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y sshpass
      - name: Install pm2
        run: npm install pm2 -g
      - name: Deploy via SSH
        run: |
          sshpass -p ${{ secrets.SSH_PASSWORD }} ssh -o StrictHostKeyChecking=no root@${{ secrets.DOMAIN }} "cd /var/www/myPetApi && git checkout . && rm -rf build && git pull && npm install && npm run build && pm2 restart build/shared/http/server.js"
